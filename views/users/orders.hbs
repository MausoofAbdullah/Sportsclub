<section>
    <div class="container"> 
        <div class="">
            {{!-- <a href="/admin/add-products" class="btn btn-success mt-5">Add Product</a> --}}
        </div>
        <table class="table mt-5 table-bordered">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">Date</th>
                    <th scope="col">Address</th>
                    <th scope="col">Mob Num</th>
                    <th scope="col">Postcode</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Payment Type</th>
                    <th scope="col">Order Status</th>
              
                </tr>
            </thead>
            <tbody>
                {{#each orders}}
                <tr>
                    <td>{{inc @index}} </td>
                    <td>{{this.date}} </td>
                    <td>{{this.deliveryDetails.address}} </td>
                    <td>{{this.deliveryDetails.phone}}</td>
                    <td>{{this.deliveryDetails.postcode}}</td>
                    <td>₹{{this.totalAmount}}</td>
                    <td>{{this.paymentType}}</td>
                    <td>{{this.status}}</td>
                    {{!-- <td><img src="/product-images/{{this._id}}.jpg" style="width: 50px; height: 50px;"></td> --}}
                    <td>
                        <a href="/view-order-products/{{this._id}}" class="btn btn-primary">VIEW PRODUCTS</a>
                    </td>
                
                    {{!-- <td><a href="/admin/edit-product/{{this._id}}" class="btn btn-primary">Edit</a>
      <a href="/admin/delete-product/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are you sure to dlete{{this.Name}}?')">Delete</a></td> --}}
    </tr>
                
                {{/each}}
            </tbody>
        </table>
    </div>

</section> 