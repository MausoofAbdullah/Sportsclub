
    <section>
        
    <div class="container"> 
        <div class=" mb-4">
            <a href="/admin/add-banner" class="btn btn-success mt-5">Add Banner</a>
        </div>
        <table class="table" id="banner">
            <thead>
                <tr>
                    
                    <th scope="col">Number</th>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                    {{!-- <th scope="col">Price</th> --}}
                    <th scope="col">Image</th>
                    <th scope="col">Option</th>
                </tr>
            </thead>
            <tbody>
                {{#each banners}}
                <tr>
                    <td>{{inc @index}} </td>
                    <td>{{this.name}} </td>
                    <td>{{this.description}} </td>
                    {{!-- <td>₹{{this.price}}</td> --}}
                    <td><img src="/product-images/{{this._id}}.jpg" style="width: 50px; height: 50px;"></td>
                    <td>
                        {{!-- <a href="/admin/edit-product/{{this._id}}" class="btn btn-primary">Edit</a> --}}
                    <a href="/admin/delete-banner/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are  you want to delete {{this.Name}} ?')">Delete</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    </div>

</section>



<script>
    $(document).ready(function () {


        $('#banner').wrap('<div id="hide" style="display:none"/>');

        $('#hide').css('display', 'block');
    });


    var table = $('#banner').DataTable();
</script>